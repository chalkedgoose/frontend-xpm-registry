import React from 'react';
import { Container  } from 'react-bootstrap';

const Landing = () => {
    return(
        <React.Fragment>
            <Container>
                <h1>Landing Page XPM</h1>
            </Container>
        </React.Fragment>
    )
}

export default React.memo(Landing)